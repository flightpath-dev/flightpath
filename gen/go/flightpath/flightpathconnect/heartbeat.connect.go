// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: flightpath/heartbeat.proto

package flightpathconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	flightpath "github.com/flightpath-dev/flightpath/gen/go/flightpath"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// HeartbeatServiceName is the fully-qualified name of the HeartbeatService service.
	HeartbeatServiceName = "flightpath.HeartbeatService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// HeartbeatServiceSubscribeHeartbeatProcedure is the fully-qualified name of the HeartbeatService's
	// SubscribeHeartbeat RPC.
	HeartbeatServiceSubscribeHeartbeatProcedure = "/flightpath.HeartbeatService/SubscribeHeartbeat"
)

// HeartbeatServiceClient is a client for the flightpath.HeartbeatService service.
type HeartbeatServiceClient interface {
	// Subscribe to HEARTBEAT messages from the drone
	SubscribeHeartbeat(context.Context, *connect.Request[flightpath.SubscribeHeartbeatRequest]) (*connect.ServerStreamForClient[flightpath.SubscribeHeartbeatResponse], error)
}

// NewHeartbeatServiceClient constructs a client for the flightpath.HeartbeatService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewHeartbeatServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) HeartbeatServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	heartbeatServiceMethods := flightpath.File_flightpath_heartbeat_proto.Services().ByName("HeartbeatService").Methods()
	return &heartbeatServiceClient{
		subscribeHeartbeat: connect.NewClient[flightpath.SubscribeHeartbeatRequest, flightpath.SubscribeHeartbeatResponse](
			httpClient,
			baseURL+HeartbeatServiceSubscribeHeartbeatProcedure,
			connect.WithSchema(heartbeatServiceMethods.ByName("SubscribeHeartbeat")),
			connect.WithClientOptions(opts...),
		),
	}
}

// heartbeatServiceClient implements HeartbeatServiceClient.
type heartbeatServiceClient struct {
	subscribeHeartbeat *connect.Client[flightpath.SubscribeHeartbeatRequest, flightpath.SubscribeHeartbeatResponse]
}

// SubscribeHeartbeat calls flightpath.HeartbeatService.SubscribeHeartbeat.
func (c *heartbeatServiceClient) SubscribeHeartbeat(ctx context.Context, req *connect.Request[flightpath.SubscribeHeartbeatRequest]) (*connect.ServerStreamForClient[flightpath.SubscribeHeartbeatResponse], error) {
	return c.subscribeHeartbeat.CallServerStream(ctx, req)
}

// HeartbeatServiceHandler is an implementation of the flightpath.HeartbeatService service.
type HeartbeatServiceHandler interface {
	// Subscribe to HEARTBEAT messages from the drone
	SubscribeHeartbeat(context.Context, *connect.Request[flightpath.SubscribeHeartbeatRequest], *connect.ServerStream[flightpath.SubscribeHeartbeatResponse]) error
}

// NewHeartbeatServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewHeartbeatServiceHandler(svc HeartbeatServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	heartbeatServiceMethods := flightpath.File_flightpath_heartbeat_proto.Services().ByName("HeartbeatService").Methods()
	heartbeatServiceSubscribeHeartbeatHandler := connect.NewServerStreamHandler(
		HeartbeatServiceSubscribeHeartbeatProcedure,
		svc.SubscribeHeartbeat,
		connect.WithSchema(heartbeatServiceMethods.ByName("SubscribeHeartbeat")),
		connect.WithHandlerOptions(opts...),
	)
	return "/flightpath.HeartbeatService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case HeartbeatServiceSubscribeHeartbeatProcedure:
			heartbeatServiceSubscribeHeartbeatHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedHeartbeatServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedHeartbeatServiceHandler struct{}

func (UnimplementedHeartbeatServiceHandler) SubscribeHeartbeat(context.Context, *connect.Request[flightpath.SubscribeHeartbeatRequest], *connect.ServerStream[flightpath.SubscribeHeartbeatResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("flightpath.HeartbeatService.SubscribeHeartbeat is not implemented"))
}
